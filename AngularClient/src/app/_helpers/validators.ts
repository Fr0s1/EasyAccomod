import { AbstractControl, ValidatorFn } from "@angular/forms";

export function stringValidator(): ValidatorFn {
    return (constrol: AbstractControl): {[key: string]: any} | null => {
        return 
    }
}